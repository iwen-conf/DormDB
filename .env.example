# DormDB 环境配置文件示例
# 复制此文件为 .env 并根据实际情况修改配置

# =============================================================================
# 服务器配置 (Server Configuration)
# =============================================================================

# 服务器监听地址 (默认: 127.0.0.1)
SERVER_HOST=127.0.0.1

# 服务器监听端口 (默认: 3000)
SERVER_PORT=3000

# =============================================================================
# SQLite 数据库配置 (SQLite Database Configuration)
# =============================================================================

# SQLite 数据库文件路径 (默认: ./dormdb_state.db)
# 用于存储申请记录和系统状态
SQLITE_PATH=./dormdb_state.db

# =============================================================================
# MySQL 数据库配置 (MySQL Database Configuration)
# =============================================================================

# MySQL 服务器地址 (必需)
# 示例: localhost, 192.168.1.100, sql.example.com
MYSQL_HOST=192.168.1.100

# MySQL 服务器端口 (默认: 3306)
MYSQL_PORT=3306

# MySQL 管理员用户名 (必需)
# 需要有 CREATE DATABASE, CREATE USER, GRANT 等权限
MYSQL_USERNAME=kaiwen

# MySQL 管理员密码 (必需)
# 请使用强密码并妥善保管
# 注意: 请勿在生产环境中使用示例密码
MYSQL_PASSWORD=your_mysql_password_here

# MySQL 默认数据库 (默认: default)
# 用于连接 MySQL 服务器的初始数据库
MYSQL_DATABASE=default

# MySQL 允许连接的主机 (默认: localhost)
# 为了安全，建议设置为具体的 IP 地址，禁止使用 %
# 示例: localhost, 192.168.1.100
MYSQL_ALLOWED_HOST=localhost

# =============================================================================
# 环境配置 (Environment Configuration)
# =============================================================================

# 开发模式 (默认: false)
# 设置为 true 时允许使用 MYSQL_ALLOWED_HOST=% (仅用于开发/测试)
# 生产环境请务必设置为 false 或不设置
DEV_MODE=true

# =============================================================================
# 管理员配置 (Admin Configuration)
# =============================================================================

# 管理员密码 (默认: admin123)
# 强烈建议在生产环境中修改此密码
ADMIN_PASSWORD=admin123

# =============================================================================
# 日志配置 (Logging Configuration)
# =============================================================================

# 日志级别设置
# 可选值: error, warn, info, debug, trace
RUST_LOG=info

# =============================================================================
# 安全配置说明 (Security Configuration Notes)
# =============================================================================

# 重要安全提醒:
# 1. MYSQL_PASSWORD 是必需的，请设置强密码
# 2. MYSQL_ALLOWED_HOST 在生产环境中不能设置为 % (通配符)
# 3. 开发环境可以设置 DEV_MODE=true 来允许 MYSQL_ALLOWED_HOST=%
# 4. 在生产环境中，建议使用专门的数据库管理账号
# 5. 定期更换密码并监控数据库访问日志
# 6. 请勿将包含真实密码的 .env 文件提交到版本控制系统

# =============================================================================
# 环境配置示例 (Environment Examples)
# =============================================================================

# 开发环境配置示例:
# DEV_MODE=true
# MYSQL_ALLOWED_HOST=%

# 生产环境配置示例:
# DEV_MODE=false  # 或不设置
# MYSQL_ALLOWED_HOST=192.168.1.100

# 测试环境配置示例:
# DEV_MODE=true
# MYSQL_ALLOWED_HOST=test.example.com
